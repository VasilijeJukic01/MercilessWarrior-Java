package platformer.model.quests;

/** This enum defines the types of objectives that can be part of a quest.
 */
public enum QuestObjectiveType {
    KILL,
    COLLECT,
    INTERACT,
    UPGRADE,
    REACH
}